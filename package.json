{
  "name": "range-proof",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Unicode block coverage proofing for fonts — runs entirely in the browser",
  "scripts": {
    "setup": "bash tools/node-setup.sh",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "bash tools/deploy.sh"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.0",
    "brotli": "^1.3.3"
  }
}
